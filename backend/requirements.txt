# DEV / full environment. Installs everything, including the OPTIONAL report
# stack, because the test suite needs it (tests/test_resample_parity.py diffs
# the vendored resampler against nilearn's via pytest.importorskip).
#
# The SHIPPED base install is requirements-frozen.txt, which no longer contains
# nilearn, matplotlib or pandas (+ the scikit-learn nilearn drags in): they are
# the optional `reports-figures` module and live in requirements-reports.txt
# (~199 MB). Nothing on the compute path imports them — figures are the only
# consumer, and both figure paths degrade with a message when they are absent.
# The three lines below marked "reports stack" are therefore duplicated from
# requirements-reports.txt on purpose; keep them in sync.

fastapi==0.110.1
uvicorn==0.25.0
python-dotenv>=1.0.1
pydantic>=2.6.4
tzdata>=2024.2
pytest>=8.0.0
black>=24.1.1
isort>=5.13.2
flake8>=7.0.0
mypy>=1.8.0
requests>=2.31.0
pandas>=2.2.0        # reports stack (optional at runtime) — requirements-reports.txt
numpy>=1.26.0
python-multipart>=0.0.9
# emergentintegrations==0.1.0  # removed: package does not exist on PyPI
SimpleITK>=2.3.0
nibabel>=5.0.0
scipy>=1.11.0
nilearn>=0.10.0      # reports stack (optional at runtime) — requirements-reports.txt
matplotlib>=3.7.0    # reports stack (optional at runtime) — requirements-reports.txt
neuropythy>=0.12.0
dipy>=1.8.0  # runtime: tract dissection workers (dissect_worker.py, dissect_between_worker.py) + data-prep scripts
dcm2niix>=1.0.20220505  # DICOM import: bundles the dcm2niix binary + puts it on PATH (Scripts/)
Send2Trash>=1.8.2  # module uninstall sends slot payloads to the recycle bin rather than deleting them outright (routers/modules.py:_remove_payload). Optional import — without it removal is permanent and the API says so.
