# Production build: the backend serves this build from the same origin, so API
# calls must be same-origin (empty base → "/api/..."). Do NOT point this at a
# host:port — that would bake a dev URL into the production bundle.
REACT_APP_BACKEND_URL=
